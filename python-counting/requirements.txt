# torch and torchvision are pre-installed in the l4t-pytorch base image — do NOT reinstall
# backports.zoneinfo: required on Python 3.8 (JetPack 5.x / Ubuntu 20.04); no-op on 3.9+
backports.zoneinfo; python_version < "3.9"
ultralytics>=8.2.0
numpy>=1.23.0,<1.25.0
lapx>=0.5.10.post1
cvzone>=1.6.0
pandas>=2.2.0
shapely>=2.0.0
psycopg2-binary
python-dotenv
paho-mqtt
onnx>=1.12.0,<2.0.0
onnxslim>=0.1.71
# onnxruntime-gpu is NOT available on PyPI for ARM64 Jetson.
# Set USE_ONNX=false (injected by dashboard compose for Jetson mode) to run
# .pt model directly on CUDA instead of exporting to ONNX.
onnxruntime
