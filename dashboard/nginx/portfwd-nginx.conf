# ============================================================
# Managed ENTIRELY by the EPiWalk dashboard — do not edit by hand.
# This file is regenerated from scratch on every device save from
# each device's PORTFWD_* vars (see dashboard/lib/portforward.ts).
#
# Container: edge-portfwd-nginx (dashboard-owned, stream-only L4 TCP
# proxy for RTSP passthrough). Bind-mounted from this file so the last
# generated config survives container restart/recreate; the committed
# version below is just the empty seed nginx boots with on a fresh host.
# ============================================================
worker_processes auto;

events {
    worker_connections 1024;
}

stream {
}
